package Types
  type Temperature = Modelica.SIunits.Temperature(start = 300);
	type Pressure = Modelica.SIunits.Pressure(start = 1e5);
	type Density = Modelica.SIunits.Density(start = 10);
end Types;
